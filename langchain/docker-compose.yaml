# Docker Compose configuration
# PostgreSQL is now using Neon cloud (see .env file)
# Local PostgreSQL service has been removed

services: {}  # No local services - using Neon cloud PostgreSQL

# Uncomment below if you want to run the API server in Docker:
# agents:
#   build:
#     context: ./app
#     dockerfile: Dockerfile
#   container_name: langchain-agents
#   environment:
#     DATABASE_URL: ${DATABASE_URL}
#     COLLECTION_NAME: rag_collection
#     LOCAL_MODEL_DIR: /app/model/llama_ko
#     LLM_PROVIDER: llama
#   ports:
#     - "8000:8000"
#   volumes:
#     - ./app/model:/app/model:ro
#   restart: unless-stopped
#   deploy:
#     resources:
#       reservations:
#         devices:
#           - driver: nvidia
#             count: 1
#             capabilities: [gpu]
